# 構成とプロバイダの比較

同じ音声エージェントを、**構成（A / B / C）** と **B の TTS プロバイダ（ElevenLabs / Deepgram / OpenAI / Google）** を
変えて作り、**遅延・費用・正しさ**で比べた記録。
数字の出どころは `results/runs.csv`（全行）と `results/<会社>/<モデル>/transcripts/*.txt`（時刻の内訳）、
`results/elevenlabs/agents-platform/raw/*.json`（ElevenLabs API の生の応答）。

最終更新: 2026-09-25（B の TTS を 4 社で比べた。A〜C は 2026-09-11 に v3 系に揃えて測り直した版）

## 3 つの構成

違いは「**何を ElevenLabs に渡し、何を手元に置くか**」だけ。

| | A: Agents Platform | B: 自前構成 | C: Knowledge Base |
|---|---|---|---|
| ノートの本文 | 手元 | 手元 | **ElevenLabs に預ける** |
| 検索の実装 | 手元（`search.py`） | 手元（`search.py`） | ElevenLabs 内部の RAG |
| 検索を呼ぶ判断 | Agent がする | **こちらが必ず先に呼ぶ** | Agent がする |
| 応答生成（LLM） | ElevenLabs 内部 | 手元から Gemini API | ElevenLabs 内部 |
| 発話（TTS） | ElevenLabs | 自分でつなぐ（4 社から選ぶ。既定は ElevenLabs） | ElevenLabs |
| 音声認識・割り込み | ElevenLabs | 自分（今はテキスト入力で回避） | ElevenLabs |
| 公開 URL / トンネル | 不要（client tool） | 不要 | 不要 |

## 構成の比較（A / B / C）

### 揃えた条件と、揃っていない条件

**揃えたもの**: 同じ 5 問、同じ検索関数、同じ声（Jessica）、同じ LLM（`gemini-3.6-flash`）、
同じ指示文（道具の有無に関する行だけ差し替え）、同じ数字の正規化。

**揃っていないもの**:

- **TTS モデルが B だけ違う。** A・C は `eleven_v3_conversational`、B は `eleven_turbo_v2_5`。
  **`eleven_v3` は stream-input（WebSocket）では 403 で拒否される**ため、
  自前でストリーミングする構成では v3 を使えない（2026-09-11 実測）。これ自体が構成の違いによる制約
- **C だけ検索が別物。** A・B は文字バイグラム、C は ElevenLabs の多言語埋め込み。
  ただし C の売りが「検索も任せる」ことなので、切り離して測ること自体ができない
- **B だけ LLM の経路が違う。** 公開 API 経由の Gemini を手元から呼ぶ。A・C は ElevenLabs の中から呼ぶ

### 結果（5 問ずつ、2026-09-11）

| 指標 | A: Agents Platform | B: 自前構成（ElevenLabs） | C: Knowledge Base |
|---|---:|---:|---:|
| 往復数 | 5 | 5 | 5 |
| 最初の音まで（中央値） | **2,213 ms** | 3,796 ms | 2,907 ms |
| 最速 | 1,908 ms | 2,765 ms | 2,548 ms |
| 最遅 | **2,287 ms** | 7,761 ms | 3,530 ms |
| ばらつき（最遅 − 最速） | **379 ms** | 4,996 ms | 982 ms |
| 言い終わりまで（中央値） | 5,774 ms | 3,969 ms | 6,659 ms |
| 1 往復のクレジット | 57 | **45** | 62 |
| 課金の単位 | 通話時間 | 合成した文字数 | 通話時間 |
| ほかに費用がかかる先 | なし | Gemini のトークン | なし |
| 正答 | 5/5 | 5/5 | 5/5 |

質問ごと（最初の音まで ms / クレジット）:

| 質問 | A | B | C |
|---|---:|---:|---:|
| デプロイ前の確認 | 2,099 / 59 | 7,761 / 58 | 2,979 / 69 |
| ロールバック | 2,213 / 59 | 2,765 / 52 | 2,907 / 65 |
| RRF とは | 2,287 / 65 | 4,343 / 50 | 2,803 / 65 |
| 5/12 の定例 | 2,282 / 60 | 3,796 / 48 | 3,530 / 70 |
| 来週の天気（ノートに無い） | 1,908 / 43 | 3,442 / 19 | 2,548 / 41 |

### 読み方

**A が最も速く、最も安定している。** 中央値 2.2 秒、ばらつきは 0.4 秒に収まる。
B は最悪 7.8 秒で、ばらつきが 5.0 秒ある。C はその中間。

**B の遅さは自前実装のせいではない。** 内訳（`results/elevenlabs/eleven_turbo_v2_5/transcripts/`）を見ると、
検索は 1〜2 ms、TTS は文を送ってから音が出るまで 357〜560 ms。
**残りは全部 Gemini の最初のトークン待ち**（2.3〜7.0 秒）。
公開 API 経由の LLM の応答時間が、そのまま体感に出ている。

**C は前回（flash 版）の 1 問だけの測定では 1.0 秒だったが、5 問流すと 2.9 秒だった。**
1 サンプルで結論を出してはいけないという例。ツール往復が無いぶん速いという理屈は
そのままだが、ElevenLabs 内部の RAG（クエリの埋め込み + ベクタ検索）にも時間がかかる。
**A の client tool（手元の検索 1〜2 ms）より、C の内部 RAG のほうが遅い。**

**「言い終わりまで」は B が短いが、これは品質差**。A・C は v3 で間の取り方が丁寧なぶん
発話が長い。待ち時間ではない。

**費用は B が安いが、請求先が 2 つに分かれる。** B の 45 クレジットは TTS の文字数ぶんだけで、
Gemini のトークン（入力 450 前後 / 出力 60 前後 / 思考 300 前後）は別勘定。

### 内訳（生の JSON から）

A の 1 往復の中身。`results/elevenlabs/agents-platform/raw/*.json` の `conversation_turn_metrics` にある。

| 段階 | 時間 |
|---|---:|
| LLM が「道具を呼ぶ」と判断するまで | 0.71 s |
| 道具の結果を受けて最初の文を書くまで | 0.88 s |
| 音声合成が最初の音を出すまで | 0.10 s |

**どの構成でも、遅延のほとんどは LLM。** 音声合成は 0.1〜2.0 秒（下の 4 社を含む）。

### モデルを替えたことによる変化

2026-09-10 の測定（TTS が `eleven_flash_v2_5`）と比べると:

| | flash_v2_5 | v3_conversational |
|---|---:|---:|
| A の最初の音（中央値） | 2,061 ms | 2,213 ms |
| A の 1 往復のクレジット | 91 | **57** |

**遅延はほぼ変わらず、費用が 4 割下がった。** 読み上げの品質も上がる（`FINDINGS_JA.md`）。
日本語で使うなら v3 に替えない理由がない。

## B の TTS プロバイダの比較（ElevenLabs / Deepgram / OpenAI / Google）

B の TTS だけを替えた。検索・prompt・Gemini の呼び方・文の切り出し・時刻の打ち方は同じ関数を通る。
書き起こしの「構成:」の行では B = ElevenLabs、D = Deepgram、E = OpenAI、F = Google と書いてある
（以前の「D: Deepgram 構成」はこの B（Deepgram））。

### 揃えた条件と、揃っていない条件

**揃えたもの**: 同じ 5 問、同じ検索、同じ prompt、同じ LLM（`gemini-3.6-flash`、思考なし）、
同じ `t0`（質問テキストを渡した時刻。TTS の接続は `t0` より前に済ませる）。

**揃っていないもの**:

- **測った日が違う。** B（ElevenLabs）は 2026-09-11、他の 3 社は 2026-09-25。Gemini の応答時間は日によってぶれるので、
  TTS の差は「最初の音」ではなく **TTS 区間**（最初の文を送ってから最初の音まで）で比べる
- **声が違う。** どれも女性の声だが、社ごとに別の声（下の表）。Jessica は他社に無い
- **届け方が違う。** ElevenLabs は WebSocket に文を流し込み、他の 3 社は 1 文 1 リクエストの REST。
  Google の REST はストリーミングせず、1 文ぶんの音をまとめて返す
- **読みの品質はまだ耳で比べていない。** 新しい 3 社の `correct` 列は空

| | ElevenLabs | Deepgram | OpenAI | Google |
|---|---|---|---|---|
| モデル / 声 | `eleven_turbo_v2_5` / Jessica | `aura-2-izanami-ja` | `gpt-4o-mini-tts` / `marin` | `ja-JP-Chirp3-HD-Kore` |
| 口 | `stream-input`（WebSocket） | `POST /v1/speak` | `POST /v1/audio/speech` | `POST /v1/text:synthesize` |
| 定価 | $0.05 / 1,000 字（Flash / Turbo） | $0.030 / 1,000 字（Aura-2 PAYG） | トークン課金（音 1 分 $0.015 の推定で換算） | $0.030 / 1,000 字（Chirp 3 HD。月 100 万字まで無料） |
| 測った日 | 2026-09-11 | 2026-09-25 | 2026-09-25 | 2026-09-25 |

### 結果（中央値、5 問を 1 回ずつ）

| 指標 | ElevenLabs | Deepgram | OpenAI | Google |
|---|---:|---:|---:|---:|
| LLM の最初のトークン | 3,067 ms | 3,166 ms | 3,166 ms | 2,402 ms |
| 最初の音まで | 3,796 ms | **3,462 ms** | 4,640 ms | 3,897 ms |
| **TTS 区間** | 443 ms | **211 ms** | 1,200 ms | 1,386 ms |
| 言い終わりまで（※） | 3,969 ms | 10,570 ms | 7,808 ms | 5,296 ms |
| 1 往復のドル（平均） | $0.0045 | $0.0029 | $0.0041 | **$0.0027** |
| 正答 | 5/5 | 未判定 | 未判定 | 未判定 |

※ 最後の音が**届いた**時刻。届き方が社ごとに違うので、並べて読まない（下の「読み方」）。

### 質問ごと

単位は ms（ドルを除く）。TTS 区間 = 最初の音 − TTS へ最初の文。

| 社 | 質問 | LLM 初トークン | TTS へ最初の文 | 最初の音 | TTS 区間 | 言い終わり | ドル |
|---|---|---:|---:|---:|---:|---:|---:|
| ElevenLabs | デプロイ前の確認 | 7,049 | 7,201 | 7,761 | 560 | 7,894 | $0.00575 |
| | RRF とは | 3,686 | 3,909 | 4,343 | 434 | 4,435 | $0.00505 |
| | 5/12 の定例 | 3,067 | 3,353 | 3,796 | 443 | 3,969 | $0.00480 |
| | 来週の天気（無い） | 2,965 | 2,965 | 3,442 | 477 | 3,591 | $0.00190 |
| | ロールバック | 2,264 | 2,408 | 2,765 | 357 | 3,077 | $0.00520 |
| Deepgram | デプロイ前の確認 | 3,611 | 3,785 | 3,994 | 209 | 12,006 | $0.00324 |
| | RRF とは | 3,166 | 3,257 | 3,462 | 205 | 10,570 | $0.00294 |
| | 5/12 の定例 | 2,721 | 3,020 | 3,276 | 256 | 8,754 | $0.00303 |
| | 来週の天気（無い） | 2,066 | 2,428 | 2,673 | 245 | 5,556 | $0.00189 |
| | ロールバック | 3,422 | 3,598 | 3,809 | 211 | 11,092 | $0.00330 |
| OpenAI | デプロイ前の確認 | 3,973 | 4,276 | 5,876 | 1,600 | 8,965 | $0.00458 |
| | RRF とは | 3,594 | 3,732 | 5,609 | 1,877 | 8,654 | $0.00400 |
| | 5/12 の定例 | 3,166 | 3,440 | 4,640 | 1,200 | 7,808 | $0.00478 |
| | 来週の天気（無い） | 2,610 | 2,610 | 3,275 | 665 | 5,653 | $0.00248 |
| | ロールバック | 2,147 | 2,283 | 3,345 | 1,062 | 7,171 | $0.00449 |
| Google | デプロイ前の確認 | 3,071 | 3,230 | 5,248 | 2,018 | 6,535 | $0.00357 |
| | RRF とは | 2,367 | 2,511 | 3,897 | 1,386 | 5,296 | $0.00324 |
| | 5/12 の定例 | 3,121 | 3,411 | 5,204 | 1,793 | 6,170 | $0.00279 |
| | 来週の天気（無い） | 2,402 | 2,576 | 3,534 | 958 | 4,162 | $0.00123 |
| | ロールバック | 2,308 | 2,450 | 3,535 | 1,085 | 5,053 | $0.00288 |

ElevenLabs のドルは送った文字数 × $0.05 / 1,000 字で換算した（文字数は ElevenLabs の決まりで文末に足す空白を含む）。

### 読み方

**TTS そのものの速さは TTS 区間で比べる。** 最初の音までの差は、ほとんどが Gemini の最初のトークンのぶれ
（Google の回は LLM が速かったぶん、最初の音が TTS 区間ほど遅く見えない）。

- **Deepgram が最速で、ぶれも小さい**（205〜256 ms）。2 本目以降のリクエストも 205〜259 ms で最初のバイトが来た
- **ElevenLabs が次**（357〜560 ms）。WebSocket を開いたまま文を流し込む
- **OpenAI は最初のバイトが遅い**（665〜1,877 ms）。応答は chunked で流れてくるが、1 本目が遅い。2 本目は 544〜891 ms
- **Google は 1 文ぶんの合成が終わるまで何も届かない**（958〜2,018 ms）。REST はストリーミングせず、
  ストリーミングの `streamingSynthesize` は gRPC にしか無い。遅いのは合成の速さではなく届け方かもしれない（切り分けていない）

**「言い終わり」は社どうしで並べない。** 最後の音が届いた時刻なので、音の届き方がそのまま出る。

| | 音の長さ | 最初の音から最後の音まで | 届き方 |
|---|---:|---:|---|
| ElevenLabs | 5〜17 秒 | 0.1〜0.3 秒 | ほぼ一括 |
| Deepgram | 9〜19 秒 | 2.9〜8.0 秒 | 実時間の約 2 倍の速さで少しずつ |
| OpenAI | 10〜19 秒 | 2.4〜3.8 秒 | 1 文ごとに数倍速で。2 本目の最初のバイト待ちが乗る |
| Google | 7〜17 秒 | 0.6〜1.5 秒 | 1 文ごとに一括。2 本目の合成時間が乗る |

どれも再生（音の長さ）の方が長いので、利用者の待ち時間にはならない。

**費用は Google と Deepgram が安い。** 文字単価はどちらも $0.030 / 1,000 字で、ElevenLabs（$0.05）の 6 割。
1 往復の平均の差（$0.0027 と $0.0029）は、返答の文字数の差（平均 91 字と 96 字）にすぎない。
**Google は Chirp 3 HD が月 100 万字まで無料**なので、この規模なら実際の請求は 0。
OpenAI はトークン課金で、ここでは音の長さ × 1 分 $0.015 の推定で出した（正は usage ダッシュボード）。
Deepgram は実行前後の残高の差が $0.00492 で、見積り $0.0144 より小さかった（反映の遅れか単価の違いか未確定）。

**日本語の声の数は社ごとに大きく違う**（Google 30、Deepgram 5、OpenAI は日本語専用の声なし）。`FINDINGS_JA.md` の 8。

**返答は 4 社とも書き起こし上は妥当。** 期待したノートを出典として口頭で添え、ノートに無い質問は「見当たりません」と言った
（LLM と prompt が同じなので当然）。読み（漢字・数字・英字略語）は耳で確かめてから `correct` 列に入れる。

## どの組み合わせが何を語るか

- **A と C**: 設定が同じで違いは検索の置き場所だけ。**ツール往復と内部 RAG の比較**になる。
  手元の検索が単純（3 ファイルのバイグラム）なので A が有利。
  実際の RAG のように検索が重い場合は逆転しうる
- **A と B**: 検索は同じだが LLM の呼び方が違う。B の遅さは「自前だから」ではなく
  「公開 API 経由の Gemini だから」
- **B 単体**: 唯一、内訳が全部見える。どこを直せばよいかが分かる
- **B の TTS の社どうし**: 検索・prompt・LLM が同じで、違いは TTS だけ。**TTS 区間の比較**になる

## いつどれを選ぶか（この題材での結論。一般化はしない）

- **速さと安定を求めるなら A。** 割り込み・無音判定・再接続を自分で書かなくてよく、それでいて速い
- **無言の時間が長い用途なら B。** A・C は通話時間で課金されるので黙っていても減る。
  B は合成した文字数ぶんなので、開いたまま放置しても増えない
- **検索も含めて任せたいなら C。** ただし本文が ElevenLabs 側に置かれ、更新のたび同期が要る
- **ストリーミングで最高品質の TTS が要るなら A か C。** `eleven_v3` は stream-input で使えない
- **B で TTS の速さを詰めるなら Deepgram、単価なら Google。** ただし Deepgram の日本語の声は 5 つだけ、
  Google は REST ではストリーミングしない。どちらも LLM の遅さはそのまま残る。読みの品質は耳でまだ比べていない

## まだ測っていないこと

割り込みの品質、電話、同時接続数、長時間の安定性、音声入力（Scribe）を通したときの遅延。
今の B はテキスト入力なので、**音声認識の時間はどの構成にも含まれていない**。
B の新しい 3 社の日本語の読み（漢字・数字・英字略語）、Google の gRPC ストリーミング、各社の別の声も未測定。

## 生データ

| 置き場所 | 中身 |
|---|---|
| `results/runs.csv` | 1 往復 1 行。追記なので過去も全部残る。`usd` と `model` の列は 2026-09-25 に追加 |
| `results/report.md` | 上の CSV から機械生成した表（`python run.py report`） |
| `results/elevenlabs/agents-platform/raw/*.json` | ElevenLabs API の応答そのまま（ターンごとの内訳、道具の引数と戻り値、LLM のトークンと単価） |
| `results/<会社>/<モデル>/transcripts/*.txt` | 返答の全文と時刻の内訳。B の他社は文ごとの応答時間も |
| `results/<会社>/<モデル>/audio/*.wav` | 録音（git には入れない） |
