# ElevenLabs のアーキテクチャと、TTS 各社の口の違い（この lab で確かめた範囲）

2026-09-11 に実際に叩いて分かったことの整理（他社の TTS との比較は 2026-09-25 に足した）。**実測したこと**と**仕様書で読んだだけのこと**を分けて書く。
コードの詳細は各モジュールの docstring に、数字は `COMPARISON.md` にある。

## 二階建て

製品は大きく 2 階建てで、**課金の単位が階ごとに違う**。ここを掴むと他が繋がる。

| | 1 階: モデル API | 2 階: Agents Platform |
|---|---|---|
| 何をくれるか | 文字 → 音声、音声 → 文字 | 「会話」そのもの |
| 状態 | 持たない。1 リクエストで完結 | 持つ。セッションが立つ |
| 誰が会話を回すか | **あなた** | **ElevenLabs** |
| 課金 | 合成した**文字数** | 通話**時間**（分） |
| この lab では | B（他社の TTS に替えても同じ階） | A と C |
| 代表的な入口 | `POST /v1/text-to-speech/{voice_id}`、WebSocket の `stream-input` | `GET /v1/convai/conversation/token`、SDK の `Conversation` |

同じ音声合成エンジンでも、階が違えば課金の単位が変わる。A が 1 往復 91 クレジット、
B が 46 だったのは技術の優劣ではなく**どこまで任せたか**の差。
黙っている時間に金がかかるかどうかも、ここで決まる。

## Agent は「設定レコード」であって、動いているプログラムではない

`setup-agent` が作る `voicelab-a` は、ElevenLabs のサーバに置かれた設定の塊。常駐プロセスではない。
中身は指示文、LLM 名、声、TTS モデル、割り込みの秒数、使ってよい道具の id、認証の要否。

会話を始めると、その設定を読んだセッションが 1 本立つ。したがって「Agent を更新する」は
設定の書き換えであり、**次の会話から効く**。

1 往復で ElevenLabs のサーバが何にどれだけ使っているかは、`results/elevenlabs/agents-platform/raw/*.json` の
`conversation_turn_metrics` から読める。A の 1 往復の実測は次のとおり。

| 段階 | 実測 |
|---|---:|
| LLM が「道具を呼ぶ」と判断するまで | 0.71 s |
| 道具の結果を受けて最初の文を書くまで | 0.88 s |
| 音声合成が最初の音を出すまで | 0.10 s |

**音声合成は 0.1 秒しか使っていない。遅延のほとんどは LLM。**
C で道具を無くしたら 2.5 秒 → 1.0 秒になったのは、この内訳と整合する（1 問だけの値。5 問では C の中央値 2.9 秒で、内部 RAG にも時間がかかった。`COMPARISON.md`）。

## 外の世界と繋ぐ 4 つの口

### 用語の定義

以下で「**会話の当事者**」と書いたものは、ElevenLabs のサーバと会話用の双方向接続を
張っているプログラムを指す。この lab では Python のプロセス、vault-web ではブラウザ、
電話では（あなたのプログラムは存在せず）電話網がそれにあたる。

「クライアント」と「サーバ」という役割の区別は、ここでは本質ではない。
会話の当事者が Web サーバの機能を持っていても構わない。**本当の違いは接続の張り方である。**

- 道具（client tool）: **既にある会話の接続に相乗りする。** 新しい接続は張られない
- 道具（webhook tool）: **ElevenLabs のサーバが新たに外向きの HTTP 接続を張る。** 会話の接続とは別物

### 一覧

| | 処理の場所 | 本文の場所 | 公開 URL | 電話 | 速さ |
|---|---|---|---|---|---|
| 道具（client tool） | 会話の当事者 | 手元 | 不要 | **不可** | 往復ぶん遅い |
| 道具（webhook tool） | URL で指定した先 | 手元 | **必要** | 可 | 往復＋ネットワーク |
| 知識ベース | ElevenLabs | **ElevenLabs** | 不要 | 可 | **最速** |
| 動的変数 | （値を渡すだけ） | - | 不要 | 可 | 影響なし |

### 道具（client tool）— この lab の A

**準備の手順**

1. あなたが ElevenLabs のサーバに、道具の**宣言**を登録する。宣言に含まれるのは、道具の名前、
   何をする道具かの説明、受け取る引数の形。**処理の中身は含まれない**
2. あなたが ElevenLabs のサーバに、Agent の設定を登録する。その設定に、手順 1 の道具の id を書く
3. あなたが Agent の設定の `client_events` に `client_tool_call` を入れる。
   **入れ忘れると道具は呼ばれない。**エラーも出ないので気づきにくい
4. あなたが、会話の当事者となるプログラムに、道具の名前と対応する関数を登録する。
   この lab では名前が `search_notes`、関数が `voicelab/search.py` の `search_notes()`

**実行時の手順**

1. 会話の当事者が ElevenLabs のサーバに接続し、会話のセッションを開く
2. 利用者が質問を発する。会話の当事者がその質問を ElevenLabs のサーバへ送る
3. ElevenLabs のサーバ上の LLM が、宣言の説明文を読み、この質問には道具が必要だと判断する
4. ElevenLabs のサーバが、**手順 1 で張られた接続を通して** `client_tool_call` を送る。
   この要求には、道具の名前、呼び出しの識別子（`tool_call_id`）、LLM が生成した引数が含まれる
5. 会話の当事者が、道具の名前をもとに自身が登録した関数を呼び出す。引数には要求に含まれていた値を渡す
6. 会話の当事者の側でその関数が実行される。この lab では手元の `corpus/` の 3 ファイルを読んで検索する
7. 会話の当事者が `client_tool_result` を送り返す。これには手順 4 の識別子、関数の戻り値、
   失敗したかどうかの真偽値が含まれる
8. ElevenLabs のサーバがその戻り値を LLM へ渡す
9. LLM が答えの文を生成する。ElevenLabs のサーバがその文を音声に変換して送る

**この方式の性質**

- ノートの本文が ElevenLabs のサーバへ渡ることは**一度もない**。往復するのは、
  LLM が組み立てた検索語（手順 4）と、見つかった抜粋（手順 7）だけ
- 手順 4 で新しい接続は張られないので、公開 URL もトンネルも要らない
- 宣言の `expects_response: true` は「結果が返るまで会話を進めない」を意味する。
  `false` なら投げっぱなしで会話が進む。`response_timeout_secs` は 1〜120 秒
- 手順 7 で `is_error: true` を返すと、LLM は「道具が失敗した」と理解して応答する

**電話で使えない理由**

手順 4 の届け先は、手順 1 で接続を張った会話の当事者である。電話の着信では、
会話の当事者は電話網であり、**あなたのプログラムはその会話に参加していない**。
相乗りする接続が存在しないので、この方式は成立しない。
会話の当事者が Web サーバの機能を持つかどうかとは無関係。

### 道具（webhook tool）— vault-web がこれ

**準備の手順**

1. あなたが ElevenLabs のサーバに、workspace secret（共有秘密）を登録する
2. あなたが ElevenLabs のサーバに、道具の宣言を登録する。宣言には名前・説明・引数の形に加えて、
   **あなたのサーバの URL**、HTTP のメソッド、ヘッダが含まれる。ヘッダは手順 1 の秘密を参照する
3. あなたが Agent の設定に、手順 2 の道具の id を書く
4. あなたが自分のサーバに、手順 2 の URL で待ち受けるエンドポイントを実装する

**実行時の手順**（手順 1〜3 は client tool と同じなので、LLM が判断した後から書く）

4. ElevenLabs のサーバが、手順 2 で登録された URL に**新たに外向きの HTTP 接続を張る**。
   接続先はあなたのサーバ。ヘッダには共有秘密、本文には LLM が生成した引数が入る
5. あなたのサーバがヘッダの共有秘密を照合し、呼び出し元が ElevenLabs であることを確認する
6. あなたのサーバが処理を実行し、結果を HTTP のレスポンスとして返す
7. ElevenLabs のサーバがそのレスポンスを LLM へ渡す。以降は client tool と同じ

**この方式の性質**

- 手順 4 で ElevenLabs のサーバがあなたのサーバへ接続するため、
  **あなたのサーバはインターネットから到達できなければならない**。
  vault-web の検証で cloudflared のトンネルを起動したのはこのため。
  トンネルの URL は起動のたびに変わるので、そのつど宣言を更新する
- 手順 4 の接続先は URL で指定される。会話に誰が参加しているかとは無関係。
  **したがって電話でも使える。**これが client tool との違い
- 手順 4 のリクエストには、利用者を識別する情報が含まれない。
  ブラウザからの要求ではないので、通常の認証トークンが付かない。
  利用者が複数いる場合、どの利用者のデータを扱うべきかを決められない。
  vault-web はこれを動的変数で解いた（次節）

**client tool と webhook tool のどちらを選ぶか**

判断基準は「**会話の当事者の側に、必要なデータと処理があるか**」。

- この lab では、Python のプロセスが会話の接続を保持し、`corpus/` も手元に持つ。
  両方が揃っているので client tool で完結する
- vault-web では、会話の接続を保持しているのはブラウザだが、**ノートは Django の backend にある**。
  接続を保持している側にデータが無いので、データのある側を別経路で呼ぶしかない。
  だから webhook を選び、その結果としてトンネルが要った
- 電話では、会話の当事者にあなたのプログラムが存在しない。webhook しか選べない

### 動的変数（dynamic variables）

**何を解く仕組みか**: webhook tool の「誰のための呼び出しか分からない」問題。

**準備の手順**

1. あなたが Agent の設定に、受け取る変数名の宣言（`dynamic_variable_placeholders`）を書く
2. あなたが webhook の道具の宣言で、送信する本文またはヘッダの中に `{{変数名}}` の差し込み位置を書く

**実行時の手順**（vault-web の例）

1. 利用者がブラウザで音声対話の画面を開き、開始のボタンを押す
2. ブラウザが vault-web のサーバへ接続情報を要求する。この要求には通常のログイン用トークンが付く。
   **したがって vault-web のサーバは、この時点では利用者が誰かを知っている**
3. vault-web のサーバが推測できない乱数を 1 つ生成し、その乱数と利用者の対応を
   自身のデータベースに保存する。有効期限は 30 分
4. vault-web のサーバが ElevenLabs から会話用トークンを取得し、それと手順 3 の乱数をブラウザへ返す
5. ブラウザが ElevenLabs のサーバへ接続する。このとき `vault_session` という名前で手順 3 の乱数を渡す
6. 会話中、LLM が検索の道具を呼ぶと判断する
7. ElevenLabs のサーバが、宣言に書かれた `{{vault_session}}` の位置に手順 5 の乱数を差し込む
8. ElevenLabs のサーバが vault-web のサーバへ HTTP リクエストを送る。
   本文には LLM が生成した検索語と、手順 7 で差し込まれた乱数が入る
9. vault-web のサーバが共有秘密を照合し、呼び出し元が ElevenLabs であることを確認する
10. vault-web のサーバが本文の乱数をデータベースで引き、対応する利用者を特定する
11. vault-web のサーバがその利用者のノートだけを検索し、結果を返す

**この設計の要**

- 手順 5 で値を渡すのはブラウザであり、**ブラウザが送る値は書き換えられうる**。
  それ単体では信用できない
- したがって乱数そのものに意味を持たせない。利用者 id を直接渡していたら、
  値を書き換えるだけで他人のノートを読める。乱数は引換券にすぎず、
  それが誰の券かを知っているのは vault-web のサーバだけ
- 呼び出し元が本当に ElevenLabs かどうかは、手順 9 の共有秘密で別に確かめる。
  **「誰の依頼か」と「呼び出し元は正当か」を別々の仕組みで担保している**

**そのほか**

- 使える場所: system prompt、最初の一言、webhook の URL・ヘッダ・本文
- `system__` で始まる変数は ElevenLabs が用意する（会話 id、発信者番号、現在時刻、通話秒数など）
- `secret__` で始めると LLM に渡らず、ヘッダにだけ使える（仕様書で読んだだけ。未検証）

### 知識ベース（Knowledge Base）— この lab の C

**準備の手順**

1. あなたが ElevenLabs のサーバへノートの本文を送信する（`POST /v1/convai/knowledge-base/text`）。
   ElevenLabs のサーバがこれを保存し、文書 id を返す
2. あなたが ElevenLabs のサーバへ索引の作成を指示する。このとき埋め込みモデルを指定する。
   **日本語のノートでは `multilingual_e5_large_instruct` を明示する。**
   既定のモデルは英語寄りで、日本語では検索が当たりにくい
3. ElevenLabs のサーバが索引を作る。この処理は即座には終わらない。
   状態は `created` → `processing` → `succeeded` の順に変わる。
   **`created` は「出来上がった」ではない。**ここを完了と誤解すると、
   索引が空のまま会話を始めてしまう（実際に踏んだ）。3 ファイルで 23 秒
4. あなたが Agent の設定に、手順 1 の文書 id と、内部検索を有効にする設定を書く。
   **道具の id は書かない**

**実行時の手順**

1. 会話の当事者が ElevenLabs のサーバに接続し、利用者の質問を送る
2. ElevenLabs のサーバが、自身が保持する索引を検索する。
   **この検索は ElevenLabs のサーバの内部で完結する。**
   会話の当事者への要求も、あなたのサーバへの要求も発生しない
3. ElevenLabs のサーバが検索結果を LLM へ渡す
4. LLM が答えを生成し、ElevenLabs のサーバが音声にして送る

**この方式の性質**

- 実行時の手順 2 で外部への往復が発生しないため、**理屈の上では 3 つの中で最も速い**。
  道具を使う方式では、この位置に 1.5〜2 秒の往復が挟まる。
  同じ質問で、道具を使う A が 2.5 秒、知識ベースの C が 1.0 秒だった（1 問）。
  ただし 5 問流すと C の中央値は 2.9 秒で A（2.2 秒）より遅く、内部 RAG の時間が往復の節約を上回った（`COMPARISON.md`）
- **索引は一度作れば ElevenLabs 側に残り、以後の会話で使い回される。**
  準備の手順 3 の 23 秒は最初の 1 回だけ。`setup-kb` を再実行しても、
  文書を名前で探して既にあれば id を使い回し、索引が `succeeded` なら作り直さない
- したがって**費用のかかる場所が移動している**。道具を使う方式は会話のたびに往復を払う。
  知識ベースは準備のときにまとめて払い、以後の会話では払わない。会話の回数が多いほど有利
- 会話の記録の `rag_usage` に、索引が引かれたかと使った埋め込みモデルが残る。
  **「速いが実はノートを見ていない」を見逃さないために必ず見る**

**代償**

- 準備の手順 1 で、**ノートの本文が ElevenLabs のサーバに保存される**
- 手元のノートを書き換えても、ElevenLabs のサーバの本文と索引は古いまま。
  **本文を更新する API は無い。**同期するには削除して入れ直す（`setup-kb --recreate`）。
  つまり 23 秒は「毎回」ではなく「内容を変えるたび」に払う
- vault-web のように毎日ノートが増える対象では、この同期の頻度が設計上の論点になる。
  内容が固定なら、ほぼ無視できる

## 観測の仕組み

会話ごとに、ElevenLabs 側に全部残る。`GET /v1/convai/conversations/{id}` で丸ごと取れる
（この lab では `voicelab/raw.py` が落として `results/elevenlabs/<構成>/raw/*.json` に保存する）。

- 全文の書き起こし（利用者の発言と Agent の発言）
- **ターンごとの内訳**（LLM の初トークンまで、TTS の初音まで、無音からの時間）
- 道具の呼び出しの引数と戻り値
- **LLM のトークン数と単価**（ドル建て）
- `rag_usage`、`cost`、`termination_reason`、警告

自分で作れば相当な仕事量になる。**ここは素直に強い。**

## 他社の TTS の口（REST とストリーミング）

B は 1 階（モデル API）の TTS だけを他社に替えられる（`README.md` の「B の TTS プロバイダ」）。
**他社のものは 1 階だけを使っていて、2 階（会話）に当たるものは使っていない。**
課金の単位もどれも 1 階と同じく合成した量なので、黙っていても減らない。

4 社の口を並べると、違いは「**1 本の接続に文を流し込めるか**」と「**音を少しずつ返すか**」の 2 点に尽きる（2026-09-25 実測）。

| | ElevenLabs | Deepgram | OpenAI | Google |
|---|---|---|---|---|
| 入口 | `wss://…/v1/text-to-speech/{voice_id}/stream-input` | `POST /v1/speak?model=<id>&encoding=linear16&sample_rate=16000&container=none` | `POST /v1/audio/speech`（`response_format=pcm`） | `POST /v1/text:synthesize`（`LINEAR16`、16kHz） |
| 認証 | `xi-api-key` ヘッダ | `Authorization: Token <鍵>` | `Authorization: Bearer <鍵>` | `x-goog-api-key` ヘッダ |
| 文の渡し方 | **1 本の接続に流し込む**（`flush: true`） | 1 文 1 リクエスト | 1 文 1 リクエスト | 1 文 1 リクエスト |
| 音の返し方 | WebSocket で少しずつ（実測ではほぼ一括で届く） | 本文をストリーミング（実時間の約 2 倍の速さ） | chunked で少しずつ | **JSON に 1 文ぶんまとめて**（base64、WAV ヘッダ付き） |
| ストリーミングの別口 | ― | ― | ― | `streamingSynthesize`（**gRPC のみ**） |
| 声とモデル | 別々に選ぶ | **1 つの id**（`aura-2-izanami-ja`） | 別々に選ぶ（`gpt-4o-mini-tts` + `marin`） | **声の名前がモデルを兼ねる**（`ja-JP-Chirp3-HD-Kore`） |
| 課金 | 文字数（クレジット） | 文字数 | トークン | 文字数（月 100 万字まで無料） |
| 残高を API で読めるか | 読める | `GET /v1/projects/{id}/balances`。**鍵に `billing:read` が無いと 403** | 読めない | 読めない（API キーでは課金を見られない） |
| TTS 区間（中央値） | 443 ms | 211 ms | 1,200 ms | 1,386 ms |

読み方:

- **ElevenLabs の `stream-input` だけが「接続を開いたまま文を足していく」口。** REST の 3 社は文ごとに
  リクエストを投げるので、2 文目以降も毎回リクエストの往復がかかる。この lab では別スレッドが文の順に 1 本ずつ投げている
- **REST でも音を少しずつ返すかどうかで最初の音が変わる。** Deepgram と OpenAI は最初のバイトが届いた時点で
  再生を始められる。Google の REST は合成が終わるまで何も返さないので、1 文目の合成時間がまるごと待ちに乗る
- **速さはストリーミングの有無だけでは決まらない。** OpenAI は流して返すが、最初のバイトまでが 0.7〜1.9 秒と長かった
- Deepgram は残高が無い状態で `/v1/speak` を叩くと **402**（`/v1/models` は通る）。無料枠が失効すると
  `balances` が空配列になる。402 の回は記録せずに止めている（音 0 の行を CSV に積むと中央値が壊れる）

## まだ確かめていないこと

仕様書で項目名を見ただけで、動かしていないもの。

- 電話（SIP、番号の紐付け、`system__caller_id`）
- 同時接続の上限とスケール特性
- 地理的な配置とデータ所在の制約
- 知識ベースの `usage_mode: 'prompt'`（RAG で引かず prompt に全部入れる）
- 自前 LLM（`custom-llm`）と on-prem の構成
- 音声クローン（IVC / PVC）の品質と手順
- 動的変数の `secret__` 接頭辞
