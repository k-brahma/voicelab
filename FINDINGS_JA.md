# 日本語の声で効いたこと（主に ElevenLabs。8 は 4 社の比較）

2026-09-11 に実測して分かったことだけを書く（8 だけ 2026-09-25）。**推測は「未確認」と明示する。**
数字の出どころは `results/` の CSV と音声、手順は各モジュールの docstring。

対象は Text to Speech と Agents Platform。日本語での利用を前提にしている。

---

## 1. モデル選びが最も効く。既定のまま使うと損をする

同じ声・同じ原稿（213 文字）で、TTS モデルだけを替えて読み上げさせ、
その音声を Scribe で書き起こして原稿と突き合わせた。**声は日本語ネイティブの共有音声で固定。**

| モデル | 一致率 | 読み違えた語 |
|---|---:|---|
| `eleven_v3` | **100.0%** | なし |
| `eleven_v3_conversational` | **100.0%** | なし |
| `eleven_turbo_v2_5` | 99.0% | 通話 |
| `eleven_flash_v2_5` | 98.5% | 通話、黙っ |
| `eleven_multilingual_v2` | 95.2% | 応答、測、通話、ノート |
| `eleven_storytel_v2` | 1.6% | 全崩壊（中国語らしき音になった） |

`eleven_multilingual_v2` は「多言語といえばこれ」に見えるが、**日本語では最下位**だった。
`応答` が「えいたい」、`測って` が「けいって」、`通話` が「どうが」になる。
`eleven_v3` ではこれらが全部正しく読まれた。

**モデルを替えるだけで読み違いはゼロになる。** 辞書も原稿の書き換えも要らなかった。

### ただし `eleven_v3` はストリーミングの口では使えない

自前でストリーミング TTS を組む場合（WebSocket の `stream-input`）、
**`eleven_v3` は 403 で拒否される**（2026-09-11 実測）。使えたのは次の 3 つ。

| モデル | stream-input | 日本語の一致率 |
|---|---|---:|
| `eleven_v3` | **403 拒否** | 100.0% |
| `eleven_turbo_v2_5` | OK | 99.0% |
| `eleven_flash_v2_5` | OK | 98.5% |
| `eleven_multilingual_v2` | OK | 95.2% |

つまり**自前構成では最高品質のモデルを使えない**。
Agents Platform 経由（`eleven_v3_conversational`）なら使える。
「自前のほうが自由が効く」と考えがちだが、**モデルの選択肢はむしろ狭くなる**。

### 会話（Agents）でも v3 が使える

Agent の `conversation_config.tts.model_id` が受け付けるのは次の 6 つ。

```
eleven_turbo_v2 / eleven_turbo_v2_5 / eleven_flash_v2 /
eleven_flash_v2_5 / eleven_multilingual_v2 / eleven_v3_conversational
```

`eleven_flash_v2_5` から `eleven_v3_conversational` に替えて実測したところ、
**遅延は悪化せず、費用はむしろ下がった。**

| | flash_v2_5 | v3_conversational |
|---|---:|---:|
| 最初の音まで | 1,871〜2,653 ms | 1,971〜2,189 ms |
| 1 往復のクレジット | 88〜100 | 59〜64 |

「言い終わりまで」は 3.1 秒 → 5.7 秒に伸びたが、これは返答の**音声が長くなった**ためで、
待ち時間が増えたわけではない。v3 は間の取り方が丁寧になる。

---

## 2. 数字の読みは prompt ではなく Agent の設定で直す

Agent が「二千二十六年五月十二日」「二十件から十件」のように**漢数字で喋る**問題に当たった。
system prompt に「算用数字で書け」と指示しても直らない。

原因は `conversation_config.tts.text_normalisation_type` の既定値だった。

| 値 | 何をするか | 日本語での結果 |
|---|---|---|
| `system_prompt`（既定） | LLM に「数字を語で書け」と指示する | **漢数字になり、読みが崩れる** |
| `elevenlabs` | 生成後に ElevenLabs 側で整形する | 算用数字のまま。読みが直る |

`elevenlabs` に変えたら解決した。遅延の増加は測定誤差の範囲（2.06 秒 → 2.06 秒）。

**日本語で Agents を使うなら、これは最初に変えておくべき設定。**

---

## 3. 発音辞書は日本語では効かなかった（未解決）

読み違いを潰そうとして、`POST /v1/pronunciation-dictionaries/add-from-rules` で
`応答` → `オウトウ` のような置換規則を 6 件登録した。結果は次のとおり。

- 自作クローンの声: 一致率 97.2% のまま変わらず。`応答` が「永代」から「英大」に変わっただけ
- 共有音声（Koji）: **音声そのものが壊れた**。Scribe が `foreign` としか書き起こさなくなった

この辞書の `alias` 方式は「別の綴りに置き換える」仕組みで、置き換えた先の
カタカナをモデルが読み直すだけなので、日本語では意味がない。
もう一方の `phoneme` 方式（発音記号を直接指定）が日本語の記号体系に対応しているかは**未確認**。

**モデルを v3 にすれば辞書は不要だった。** 先に試すべきはモデルの変更。

---

## 4. 声を替えても読み違いは直らない

読み違いが「声の癖」なのか「モデルの問題」なのかを切り分けた。
同じ原稿を 4 つの声（自作クローン + 日本語ネイティブの共有音声 3 つ）で読ませた。
モデルはすべて `eleven_multilingual_v2`。

| 声 | 一致率 | 相違 |
|---|---:|---:|
| Koji（業務朗読向け） | 98.0% | 3 |
| 自作クローン | 97.2% | 3 |
| Takuya | 96.2% | 4 |
| Tsuyuri | 95.2% | 4 |

**`応答` と `黙っ` は 4 つ全部で崩れた。** 声を替えても同じ語が同じように崩れるなら、
原因は音色ではなく、文字から音を作る段階にある。この切り分けがモデル変更に繋がった。

日本語ネイティブの声を選んでも、モデルが古ければ読み違いは残る。

---

## 5. 声のクローンは「量」ではなく「録音レベル」で決まる

Instant Voice Cloning の公式要件は **最低 1 分、推奨 1〜2 分、3 分超は逆効果**。
手元には 172 件・8.5 分の録音があったので、量は最初から足りていた。
問題は品質のほうだった。

### 振り切れた録音は使えない

原音 172 件のピークを測ったところ:

| ピーク | 件数 | 割合 |
|---|---:|---:|
| 0 dB 以上（振り切れ） | 65 | 38% |
| -1〜0 dB（際どい） | 74 | 43% |
| -1 dB 未満（安全） | 33 | 19% |

ブラウザ録音の入力レベルが高すぎた結果。**0 dB を超えたものでクローンを作ると、
生成音声が歪む。** 耳で「割れている」と判定した 8 件は、全てピークが 0 dB 以上だった
（中央値 +0.1 dB）。良いと判定した 14 件は中央値 -0.1 dB。**判定と測定が一致した。**

### やってはいけない前処理

最初、素材を `loudnorm`（EBU R128 のラウドネス正規化）で揃えた。これは**動的な圧縮**を
かけるので、音声では歪みが出る。静的な音量調整（`volume` フィルタ）で十分だった。

```bash
# 悪い例: 動的圧縮が入る
ffmpeg -i in.webm -af loudnorm=I=-20:TP=-3:LRA=7 -ar 48000 -ac 1 -c:a pcm_s16le out.wav

# 良い例: 測ってから静的に持ち上げる。ピークが -3 dB を超えないよう抑える
ffmpeg -i in.webm -af volume=+4.20dB -ar 48000 -ac 1 -c:a pcm_s16le out.wav
```

### 加工後のファイルで選んではいけない

アプリが保存していた「加工後の WAV」は音量が下げられていたので、
**原音が振り切れていても数値はきれいに見えた。** 選別は必ず原音を測って行う。
加えて、加工後は 22.05 kHz に落ちていた（原音は 48 kHz）。素材は原音から作る。

### ffmpeg の `Peak count` は当てにならない

音割れの判定に astats の `Peak count`（最大値に張り付いたサンプル数）を使おうとしたが、
**どのファイルでも 2 が返り、判定に使えなかった。**
`Peak level dB` が 0 以上かどうかだけを見ればよい。

### 上限は 25 ファイル

`POST /v1/voices/add` に 26 本送ったら `too_many_samples` で 400 が返る。
公式は「本数ではなく合計時間が重要」と書いているが、**本数にも上限がある**。

---

## 6. 日本語だけで作ったクローンは、他言語に訛る

日本語の朗読 25 本（117 秒）でクローンを作り、英語とスペイン語を喋らせた。
**読み違いは起きない**（下記）が、日本語話者の発音になる。

| 言語 | 単語一致率 | 相違 |
|---|---:|---:|
| 英語 | 100.0% | 0 / 90 語 |
| スペイン語 | 100.0% | 0 / 110 語 |

音色と言語は分離しているので、日本語だけの素材でも他言語を喋れる。
ただし**発音の癖も一緒に写る**ので訛りは残る。減らすには素材に他言語を混ぜる（未検証）。

なお、この一致率の比較は**日本語に不利**である点に注意。英語・スペイン語は単語単位で
比べているので多少の訛りがあっても一致するが、日本語は文字単位なので 1 文字ずれれば
相違として数える。

---

## 7. 読み上げ品質を機械的に検査する方法

耳で全部聞くのは現実的でないので、**生成音声を Scribe で書き起こして原稿と突き合わせた**。

```python
pcm = client.text_to_speech.convert(voice_id, text=原稿, model_id=モデル,
                                    output_format='pcm_24000', language_code='ja')
# WAV に書き出してから
heard = client.speech_to_text.convert(model_id='scribe_v1', file=fh, language_code='jpn').text
ratio = difflib.SequenceMatcher(None, 正規化(原稿), 正規化(heard)).ratio()
```

**限界がある。** 音声認識は文脈から補正するので、

- 実際は正しく読めているのに別の語に書き起こすことがある
  （`ノート` → `脳と` は本人が聞いて「問題ない」と判断した = 認識側の誤り）
- 逆に、聞き取りにくかっただけの箇所を正しい漢字に直してしまうこともある

したがって**この数値は下限の目安**であり、最終判断は耳で行う。
ただし「全部の声で同じ語が崩れる」のような**傾向の検出**には十分使える。

---

## 8. 比較: 他社の TTS で使える日本語の声

B（自前構成）の TTS を 4 社で替えて測った（2026-09-25、`COMPARISON.md`）。声の選択肢は社ごとに大きく違う。
一覧は各社の API か公式ガイドで 2026-09-25 に確かめた。

| 社 | 日本語の声 | 確かめ方 | この lab の既定 |
|---|---|---|---|
| ElevenLabs | premade のうち日本語が検証済みなのは George / Alice / Jessica。日本語ネイティブの共有音声（Koji・Takuya・Tsuyuri など）もある。ネイティブの「Aya」は Starter 以上 | `GET /v1/voices` の `verified_languages` | Jessica |
| Deepgram | **Aura-2 の 5 声だけ**。女性 `aura-2-izanami-ja` / `aura-2-uzume-ja` / `aura-2-ama-ja`、男性 `aura-2-fujin-ja` / `aura-2-ebisu-ja`。旧 Aura は英語のみ、Flux の TTS は一覧に無い | `GET /v1/models` の `languages` | `aura-2-izanami-ja` |
| OpenAI | 組み込み 13 声（alloy / ash / ballad / coral / echo / fable / nova / onyx / sage / shimmer / verse / marin / cedar）。**日本語専用の声は無く**、どの声も多言語で話す。音質で推されているのは marin と cedar | 公式の TTS ガイド | `marin` |
| Google | **Chirp 3 HD の 30 声**（女性 14・男性 16）。名前は `ja-JP-Chirp3-HD-<名>`。ja-JP 全体では 41 声 | `GET /v1/voices?languageCode=ja-JP` | `ja-JP-Chirp3-HD-Kore` |

- 声の一覧と既定を選んだ理由は `.env.example` と各モジュールの説明（`deepgram_path.py` / `openai_path.py` / `google_path.py`）にある
- ElevenLabs で分かったこと（1〜4）は「**読み違いは声ではなくモデルで決まる**」だった。他社でも同じかは**未確認**
- 他社の読みの品質（漢字の読み違い、数字、英字略語）は**まだ比べていない**。上の 7 の検査（Scribe で書き起こして
  突き合わせる）を各社の録音（`results/<会社>/<モデル>/audio/`）にかけるのが次の一手

---

## まとめ: 日本語で使うときの手順

1. **モデルを `eleven_v3` 系にする。** 既定や `eleven_multilingual_v2` のままにしない
2. Agents なら **`text_normalisation_type` を `elevenlabs` にする。** 数字が漢数字にならない
3. クローンの素材は **原音を測り、ピークが 0 dB 以上のものを捨てる。** 動的圧縮をかけない
4. 読み違いが残ったら、**辞書より先にモデルを疑う**
5. 検査は Scribe で書き起こして突き合わせる。ただし数値は目安、最後は耳
